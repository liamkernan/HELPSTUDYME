import React from "react";
import {ArrowLeft} from "lucide-react";

export default function StudyMaterial({ onBack }) {
    return (
        <h1 className="text-white mt-28 text-4xl font-bold">STUDY MATERIAL</h1>
    );
}